import tkinter as tk
import sys
global counter
counter=0
def onclick(counter)
  counter.set(counter.get()+1)
  n1.config(text = counter)
def onclick2(counter)
   if counter>0
  counter.set(counter.get()-1)
  n2.config(text = counter)
  else:
        print("a")
def onclick3(counter)
    counter=0
    n3.config(text = counter)
root=tk.Tk()
n1=tk.Button(root,text="increase",command=lamda:onclick(counter))
n2=tk.Button(root,text="decrease",command=lamda:onclick2(counter))
n3=tk.Button(root,text="reset",command=lamda:onclick3(counter))

root.mainloop()
